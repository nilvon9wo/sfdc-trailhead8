<aura:component >
	<aura:registerEvent name="compEvent" type="c:notif_compEvent" />
	
	<h1>Simple Component Event Sample</h1>
	<p>
		<ui:button label="Click here to fire a component event" press="{!c.fireComponentEvent}" />
	</p>
</aura:component>